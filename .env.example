# Create a new Supabase project and fill these in
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# MetaCopier API key is NOT stored here — it is stored per-user in Supabase Vault
# via the save-api-key edge function.
